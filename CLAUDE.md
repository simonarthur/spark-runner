Use type hinting on Python code.
Any time a bug is encountered or a new feature is added, be sure to add a unit test.
Perform a local git commit after every change.
Use the Python venv in .venv for any test runs, syntax checks, etc.
Make sure that unit tests do not create a ~/spark-runner directory.